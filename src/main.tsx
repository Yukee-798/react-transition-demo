import React from "react";
import ReactDOM from "react-dom/client";
import _ReactDOM from "react-dom";
import App from "./App";
import "./main.scss";

ReactDOM.createRoot(document.getElementById("root")!).render(
  // <React.StrictMode>
  <App />
  // </React.StrictMode>
);

// _ReactDOM.render(<App />, document.getElementById('root')!);
